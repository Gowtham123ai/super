# Dependencies
node_modules/
dist/
target/
bin/

# IDE files
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# Local Env
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Maven
.mvn/
maven/
!backend/maven/ # But we might want to keep the local maven if it's there? No, usually not. But the user has a local maven there in backend/maven. If they want it on github, I should not ignore it. But usually it's better to use mvnw.
# Wait, the local maven is in backend/maven/apache-maven-3.9.6. Should I include it? it's huge. I'll ignore it.

backend/maven/
